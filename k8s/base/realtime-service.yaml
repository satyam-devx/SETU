apiVersion: v1
kind: Service
metadata:
  name: setu-realtime
  namespace: setu
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: setu-realtime
  ports:
    - name: http
      port: 8787
      targetPort: http
